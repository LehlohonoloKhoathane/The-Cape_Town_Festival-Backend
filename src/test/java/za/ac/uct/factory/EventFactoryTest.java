package za.ac.uct.factory;
/*
package za.ac.uct.factory;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;
import za.ac.uct.domain.Event;
import za.ac.uct.domain.PriceGroup;
import za.ac.uct.factory.impl.EventFactory;

class EventFactoryTest {
    @Test
    void testCreateEvent() {
        EventFactory factory = new EventFactory();
        Event event = factory.createEvent(1, "Toyota", "Corolla", 2022, "Compact", PriceGroup.ECONOMY, "ABC 123 GP", true);

        Assertions.assertNotNull(event);
        Assertions.assertEquals(1, event.getId());
        Assertions.assertEquals("Toyota", event.getMake());
        Assertions.assertEquals("Corolla", event.getModel());
        Assertions.assertEquals(2022, event.getYear());
        Assertions.assertEquals("Compact", event.getCategory());
        Assertions.assertEquals("ABC 123 GP", event.getLicensePlate());
        Assertions.assertEquals(true, event.isAvailable());
    }

}*/
